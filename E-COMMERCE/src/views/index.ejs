<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title%>
    </title>
    <link rel="stylesheet" href="/index.css">
</head>

<body>
    <nav>
        <a href="/product" target="_self">Create new product</a>
    </nav>
    <div class="tileDiv" >
        <% products.forEach(elem=> { %>
            <div class="tile">
                <div class="imageDiv">
                    <img src="<%= elem.image %>" alt="rolex watch">
                </div>
                <a href="/productDetail/<%= elem._id %>" target="_self"><h1><%= elem.title %></h1></a>
                <h5><%= elem.description %></h5>
                <h2><%= elem.price %></h2>
                <p><%= elem.category %></p>
            </div>
        <% }); %>
    </div>

</body>

</html>